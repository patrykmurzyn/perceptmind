{
  "name": "perceptmind",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "npm run lint && npm run build",
    "lint": "npx tsc --noEmit --pretty false",
    "test": "npm run build",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "gsap": "^3.14.2"
  }
}
